<footer class="footer">
  <div class="container">
    <span>© {new Date().getFullYear()} PROJECT:automate</span>
    <span>Architectural lighting and outdoor technology · Los Angeles</span>
    <a href="tel:+13107405375">(310) 740-5375</a>
  </div>
</footer>

<style>
  .footer {
    background: #0b1016;
    color: #cfc8bd;
    padding: 1.5rem 0;
  }

  .container {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    font:
      0.68rem/1.5 Inter,
      Arial,
      sans-serif;
    gap: 0.75rem 2rem;
    justify-content: space-between;
    letter-spacing: 0.04em;
    margin-inline: auto;
    width: min(1200px, calc(100% - 80px));
  }

  .footer a {
    color: #fff;
    font-weight: 700;
    text-decoration: none;
  }

  @media (max-width: 900px) {
    .container {
      width: min(1200px, calc(100% - 40px));
    }
  }

  @media (max-width: 680px) {
    .container {
      align-items: flex-start;
      flex-direction: column;
    }
  }
</style>
